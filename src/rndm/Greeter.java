package Archive.rndm;

/** Hier ist ein Beispiel für eine Java-Datei, die vorgegeben ist und
 verändert eingereicht werden soll.

 Das ganze funktioniert genau so wie für die Übungsaufgaben mit
 folgenden Unterschieden:

 - Alle vorgegebenen Dateien befinden sich in der mit der Angabe
 und dem Antwortbogen in der zip-Datei!
 -

 Bitte beachte:

 - Nachdem Hilfsmittel -- dazu zählen z.B. IntelliJ oder
 Eclipse -- zugelassen sind, müssen wir Syntaxfehler konsequent
 bestrafen. D.h. Dein Code muss wenigstens compilieren!

 - Neben einem Grundgerüst für Java-Code könnten -- wie in den
 Übungen -- auch Textdateien einzureichen sein für alles, was
 nicht mit Java zu tun hat aber auch nicht so gut in ein Textfeld
 im PDF-Antwortbogen passt (z.B ein Beweis).

 In diesem Fall kannst Du kannst -- musst aber nicht -- Markdown
 verwenden, ebenfalls wie in die Übungen.

 Alle mathematischen Sonderzeichen kannst Du mit ASCII-Zeichen
 darstellen. Beispiele sind Implikation "=>" statt "⇒", "n^2"
 statt "n²", "log_2" statt "log₂" und logische Operatoren "&&"
 wie in Java statt "∧", "||" statt "∨" bzw. "!" statt "¬"). Du musst
 Dir also keine Sonderzeichen aus dem Unicode-Zeichensatz suchen
 oder gar LaTeX lernen!

 Viel Erfolg!
 */
class Greeter {
    public static void main(String[] args) {
        System.out.println("Hallo Philipp!");
    }
}
